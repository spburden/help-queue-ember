
import { openTickets } from 'help-queue/helpers/open-tickets';
import { module, test } from 'qunit';

module('Unit | Helper | open tickets');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = openTickets([42]);
  assert.ok(result);
});

