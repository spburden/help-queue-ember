
import { timeDifference } from 'help-queue/helpers/time-difference';
import { module, test } from 'qunit';

module('Unit | Helper | time difference');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeDifference([42]);
  assert.ok(result);
});

